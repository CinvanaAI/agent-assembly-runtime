# Origin

Extracted from `Operations/assembly` in the historical Python Agent Foundry workbench. The public version replaces the host `CollectionHandoff` dependency with an explicit caller-selected assemblies directory while preserving the canonical schema, normalization, validation, projection, and memory machinery.
